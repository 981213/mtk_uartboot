[package]
name = "mtk_uartboot"
version = "0.1.0"
edition = "2021"
license = "GPL-2"

[profile.release]
panic = "abort"
strip = true

[dependencies]
clap = { version = "4.4.18", features = ["derive"] }
clap-num = "1.0.2"
serialport = { version = "4.3", default-features = false }
